@model IEnumerable<SENAI.BrownFit_Thanara.Models.Relatorio>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.RelatorioID }) |
                @Html.ActionLink("Details", "Details", new { id=item.RelatorioID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.RelatorioID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
